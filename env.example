# Umami Analytics Configuration
# These are used by the @danielgtmn/umami-react package
# Note: For Vite, variables must be prefixed with VITE_ to be exposed to the client
VITE_UMAMI_URL=https://um.kairo.click
VITE_UMAMI_ID=e1767cc6-3e0b-4ab0-b1fe-701b92a8741e

# Optional: Force enable/disable analytics regardless of environment
# If not set, analytics will only load in production builds
# VITE_ENABLE_ANALYTICS=true

# Environment Information (Informational)
# Vite uses --mode to determine the build environment (development, production)
# NODE_ENV=production

